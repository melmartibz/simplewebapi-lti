@model List<Category>

<div class="card card-plain cardfile-container-list">
    <div class="card-header card-header-primary">
        <h4 class="card-title mt-0">List of Categories</h4>
    </div>
    @*<div class="card-header cardfile-header">
        <div class="row">
            <div class="col-12">
                <button type="button" class="btn btn-outline-mdb-color waves-effect cardfile-btn cardfile-crudbtn">
                    <i class="fas fa-user-plus fa-sm pr-2"
                       aria-hidden="true"></i>
                    Add Category
                </button>
            </div>
        </div>
    </div>*@
    <div class="card-body">
        <div class="table-responsive">
            <table id="cf_list_tblfaculty" class="table table-sm table-bordered table-hover table-striped cardfile-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>NAME</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Count > 0)
                    {
                        @foreach (var e in Model)
                        {
                    <tr>
                        <td>@e.ID</td>
                        <td>@e.Name</td>
                        @*<td>
        <a asp-action="Edit" asp-route-id="@e.ID" class="btn btn-outline-primary">Edit</a>
        <a asp-action="Delete"
           asp-route-id="@e.Id"
           class="btn btn-outline-danger"
           onclick="return confirm('Are you sure to remove this record?')">Delete</a>
                        </td>*@
                    </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


